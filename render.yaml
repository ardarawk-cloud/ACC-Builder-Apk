services:
  - type: web
    name: kin-api-alpha
    runtime: docker
    plan: free
    rootDir: kin/backend
    dockerfilePath: ./Dockerfile
    dockerContext: .
    healthCheckPath: /health
    envVars:
      - key: KIN_ENVIRONMENT
        value: production
      - key: KIN_DATABASE_URL
        fromDatabase:
          name: kin-postgres-alpha
          property: connectionString
      - key: KIN_JWT_SECRET
        generateValue: true
      - key: KIN_ACCESS_TOKEN_MINUTES
        value: "30"
      - key: KIN_REFRESH_TOKEN_DAYS
        value: "30"

databases:
  - name: kin-postgres-alpha
    plan: free
    databaseName: kin
    user: kin
